import { defineConfig } from 'vite';

export default defineConfig({
  // Relative asset paths make the built site work on Hostinger, GitHub Pages,
  // local previews, and any domain/subfolder without extra configuration.
  base: './',
});
